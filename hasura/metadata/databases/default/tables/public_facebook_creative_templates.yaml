table:
  name: facebook_creative_templates
  schema: public
select_permissions:
  - role: user
    permission:
      columns:
        - created_at
        - creatomate_template_id
        - description
        - id
        - json_schema
        - name
        - ui_schema
        - updated_at
      filter:
        enabled:
          _eq: true
      allow_aggregations: true
    comment: ""
